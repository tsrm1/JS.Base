// импортируем модуль sumNumbers из файла modulOne.mjs в текущей директории ./
import sumNumbers from './modulOne.mjs'

const res1 = sumNumbers(10, 2)
console.log('Результат функции sumNumbers = ', res1)
const res2 = sumNumbers(10, 20)
console.log('Результат функции sumNumbers = ', res2)
